// URL pública da API depois de hospedada (ex: Render, Railway).
// Em desenvolvimento local, deixe como está — o padrão em api.js já aponta
// para http://127.0.0.1:8000.
//
// Depois de hospedar o backend, troque a linha abaixo pela URL real, ex:
// window.GYMFLOW_API_URL = "https://gymflow-api.onrender.com";
window.GYMFLOW_API_URL = "";
